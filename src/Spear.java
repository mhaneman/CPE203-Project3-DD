import processing.core.PImage;

import java.util.List;

public class Spear extends Entity
{

    public Spear(String id, Point position, List<PImage> images) {
        super(id, position, images);
    }
}
