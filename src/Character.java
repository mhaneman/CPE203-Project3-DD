import processing.core.PImage;

import java.util.List;

public class Character extends Entity
{

    public Character(String id, Point position, List<PImage> images) {
        super(id, position, images);
    }
}
